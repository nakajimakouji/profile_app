<div class="top">
  <p>自己紹介を編集する</p>
</div>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <p>自己紹介文</p>
    <%= form_with(model: @user, local: true, url:@user ) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.text_field :profile_text %>
      <%# TODO: ボーダーラインを入れる %>
      <p>50文字以上、200文字未満で入力してください</p>

      <span class="image">
        <%= f.file_field :avatar_image %>
      </span>

      <%= f.submit "自己紹介を確定する", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>